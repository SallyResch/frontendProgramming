//CALLBACK FUNCTIONS
/*
const sayName = name => {
    return (`Hello ${name}`)
}

const sayGoodbye = name => {
    return (`Goodbye ${name}`)
}

const getName = (callbackFunction) => {
    let userName = prompt("Enter your name");
    let response = callbackFunction(userName);
    console.log(`This is the response: ${response}`)
}
getName(sayName);
getName(sayGoodbye);
*/

